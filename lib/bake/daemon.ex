defmodule Bake.Daemon do
  def install do
    # TODO Fetch the current bake_daemon app.
    #  Place it in BAKE_HOME
  end

  def start() do
    # Start the bakeware deamon
  end

  def stop() do
    # Stop the bakeware daemon
  end
end
